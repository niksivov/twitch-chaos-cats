/**
 * GameEvent описывает события игры с типом и текстом.
 *
 * ⚠️ Сейчас не используется напрямую в логах.
 * Может понадобиться в будущем для:
 * - умного отображения в UI (разные стили для ROUND_START, BOOSTER_USED и т.д.)
 * - интеграции с Twitch-чатом
 * - аналитики и реплеев матчей
 * - системы триггеров, достижений, подсветки моментов
 */
export interface GameEvent {
  id: string

  type: string

  text: string

  createdAt: number
}